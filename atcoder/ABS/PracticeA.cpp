#include <bits/stdc++.h>

using namespace std;
using ll = long long;

#define REP(i, n) for (int i = 0; i < (n); i++)

int main() {
    cin.tie(0);
    ios_base::sync_with_stdio(false);
    int a, b, c;
    string s;
    cin >> a;
    cin >> b >> c;
    cin >> s;
    cout << (a + b + c) << " " << s << endl;
    return 0;
}