#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main() {
    int N; cin >> N;
    vector<ll> w(N), v(N);
    ll W;

    for (auto& x:w) cin >> x;
    for (auto& x:v) cin >> x;
    cin >> W;



}